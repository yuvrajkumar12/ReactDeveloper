
export default function boxname({name,Textcolor}){
    return(
        <div>
            <h1 style={{color:Textcolor}} >Namr:{name}</h1>
            <p style={{color:Textcolor}}>COlor:{Textcolor}</p>
        </div>
    );
}